#include <stdio.h>

/*
 * main - Entry point for capital and lowercase letters
 *
 * Return: Always 0
 */

int main(void)
{
char c;

char c = 'a' || c = 'A';
while (char c <= 'z' || 'Z')

putchar(c);
c++;

putchar('\n');
return (0);
}
